// import { createProxyMiddleware } from 'http-proxy-middleware';

// app.use('/login', createProxyMiddleware({ target: 'http://localhost:3100', changeOrigin: true }));
// // app.use(createProxyMiddleware('/login', { target: 'http://localhost:9000', changeOrigin: true }));